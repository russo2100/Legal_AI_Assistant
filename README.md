# Legal AI Assistant (RU) — Юридический RAG-агент

> Версия: 1.0.0 | Фреймворк: LangGraph | Юрисдикция: РФ

Интеллектуальный юридический ассистент для поиска нормативных актов и судебной практики РФ с использованием RAG (Retrieval-Augmented Generation) и оркестрацией через LangGraph.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv venv
.\venv\Scripts\Activate.ps1  # PowerShell
# или
.\venv\Scripts\activate.bat  # CMD

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
# Копирование шаблона
Copy-Item .env.example .env

# Заполните .env следующими переменными:
# OPENROUTER_API_KEY=ваш_ключ
# DATABASE_URL=postgresql://user:pass@localhost:5432/yurik_db
```

### 3. Запуск

```bash
# Запуск агента с запросом
python src/main.py "Статья 330 ГК РФ неустойка"

# Запуск тестов
pytest src/tests -v

# Индексация кодексов
python src/rag/indexer.py --codes data/codes
```

## 📁 Структура проекта

```
yurik/
├── src/
│   ├── graph/           # LangGraph workflow
│   │   ├── state.py     # AgentState (TypedDict)
│   │   ├── nodes.py     # Узлы графа (classify, search, generate...)
│   │   └── workflow.py  # Сборка графа
│   ├── rag/             # RAG-компоненты
│   │   ├── embeddings.py # OpenRouter embedder
│   │   ├── indexer.py   # Загрузка кодексов в pgvector
│   │   └── retriever.py # Гибридный поиск (BM25 + vector)
│   ├── tools/           # Инструменты
│   │   ├── kad_parser.py # Парсер kad.arbitr.ru
│   │   ├── citation_check.py # Проверка цитирования
│   │   └── legal_utils.py # Вспомогательные функции
│   ├── llm/             # LLM-клиенты
│   │   ├── client.py    # OpenRouter client
│   │   └── prompts.py   # Jinja2 шаблоны
│   ├── prompts/         # Prompt-шаблоны (.j2)
│   ├── tests/           # Тесты
│   └── main.py          # CLI точка входа
├── data/
│   ├── codes/           # Тексты кодексов
│   └── cache/           # Кэш парсинга
├── docs/                # Документация
├── logs/                # Логи приложения
└── docker/              # Docker конфигурация
```

## 🔧 Архитектура

### Workflow графа

```
START → classify_query → search_norms → search_cases → generate_answer → verify_citation → END
```

### Компоненты

| Компонент | Описание |
|-----------|----------|
| **classify_query** | Классификация запроса по типу права |
| **search_norms** | Поиск нормативных актов (RAG) |
| **search_cases** | Поиск судебной практики (kad.arbitr.ru) |
| **generate_answer** | Генерация ответа через LLM |
| **verify_citation** | Проверка цитирования источников |

### LangGraph Integration

Проект использует **LangGraph 1.0.3** — низкоуровневую оркестрационную платформу.

**Соответствие лучшим практикам:**
- ✅ StateGraph с TypedDict (`src/graph/state.py`)
- ✅ Узлы как функции (`src/graph/nodes.py`)
- ✅ START/END из нового API (`src/graph/workflow.py`)
- ✅ Durable execution через передачу state
- ✅ Логирование каждого узла для отладки

📄 **Подробно:** см. `docs/langgraph-integration.md`

## 📋 Тестирование

```bash
# Все тесты
pytest src/tests -v

# Конкретный модуль
pytest src/tests/test_nodes.py -v

# E2E тесты на golden set
pytest src/tests/test_e2e.py -v

# С покрытием
pytest --cov=src src/tests
```

## 🐳 Docker (опционально)

```bash
# Запуск PostgreSQL с pgvector
docker compose -f docker/docker-compose.yml up -d

# Проверка
docker compose ps
```

## 🔑 API-ключи

### OpenRouter

1. Зарегистрируйтесь на https://openrouter.ai
2. Создайте API-ключ
3. Добавьте в `.env`:
   ```
   OPENROUTER_API_KEY=sk-or-...
   OPENROUTER_MODEL=qwen/qwen-2.5-72b-instruct
   ```

### PostgreSQL

Для локальной разработки:
```bash
# Docker
docker run -d --name yurik-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=yurik_db \
  -p 5432:5432 \
  pgvector/pgvector:pg16
```

## 📝 Примеры использования

```bash
# Поиск нормы по статье
python src/main.py "Статья 330 ГК РФ"

# Вопрос о неустойке
python src/main.py "Как взыскать неустойку по договору поставки?"

# Поиск дела по номеру
python src/main.py "Дело А40-123456/2024"

# Консультация по договору
python src/main.py "Расторжение договора аренды односторонне"
```

## ⚠️ Дисклеймер

> **Аналитическая подсказка. Требуется проверка юристом.**

Проект предоставляет информационную поддержку и не заменяет профессиональную юридическую консультацию.

## 📄 Лицензия

MIT

## 📞 Контакты

- Документация: `docs/`
- Лог ошибок: `docs/error-log.md`
- Прогресс: `docs/progress.md`
