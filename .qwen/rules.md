# Правила проекта Legal AI Assistant

## 🔒 Критические правила

### ЗАПРЕТ НА ПУШ (PUSH BAN)
> **⛔ СЛЕДУЮЩИЕ ИЗМЕНЕНИЯ НЕ ПУШАТЬ В GITHUB БЕЗ ЯВНОЙ КОМАНДЫ ПОЛЬЗОВАТЕЛЯ**

Это правило имеет наивысший приоритет. Никакие изменения не отправляются в удалённый репозиторий (`git push`) без прямой команды:
- «push в github»
- «отправь изменения в репозиторий»
- «запуш изменения»

**Исключения:** Нет. Только явная команда пользователя.

---

## 📁 Структура проекта

```
yurik/
├── src/                    # Исходный код
│   ├── graph/             # LangGraph workflow
│   ├── rag/               # RAG-компоненты
│   ├── tools/             # Инструменты
│   ├── llm/               # LLM-клиенты
│   └── tests/             # Тесты
├── skills/                # Навыки (skills)
│   ├── legal-research/    # Поиск норм и практики
│   └── contract-draft-assist/ # Создание документов
├── docs/                  # Документация
├── data/                  # Данные (кодексы, кэш)
└── logs/                  # Логи (не коммитить!)
```

---

## 🧠 Навыки (Skills)

### Активные навыки

| Навык | Файл | Назначение |
|-------|------|------------|
| **legal-research** | `skills/legal-research/SKILL.md` | Поиск нормативных актов и судебной практики РФ |
| **contract-draft-assist** | `skills/contract-draft-assist/SKILL.md` | Создание и проверка юридических документов |

### Когда использовать

- **legal-research**: Пользователь спрашивает о законах, статьях, судебной практике
- **contract-draft-assist**: Пользователь просит создать/проверить документ (договор, иск, претензия)

---

## 📝 Стиль кода

### Python
- Типизация: `def func(arg: str) -> dict:`
- Docstrings: Google-style для публичных функций
- Логирование: `logger.info()` вместо `print()`
- Обработка ошибок: `try/except` с конкретными исключениями

### Git
- Commit messages: осмысленные, на английском
- Формат: `type(scope): description`
- Пример: `feat(skills): Add contract-draft-assist skill`

---

## 🔐 Безопасность

- API-ключи только через `.env` (не коммитить!)
- Логирование всех запросов в `logs/audit.log`
- Дисклеймер в каждом ответе: *«Аналитическая подсказка. Требуется проверка юристом.»*
- Санитизация входных данных (защита от prompt-injection)

---

## 🧪 Тестирование

Перед любыми изменениями:
```bash
pytest src/tests -v
```

**Требование:** Все тесты должны проходить (49/49).

---

## 📚 Документация

| Файл | Назначение |
|------|------------|
| `README.md` | Основная документация проекта |
| `docs/architecture.md` | Архитектура системы |
| `docs/langgraph-integration.md` | Интеграция LangGraph |
| `docs/tz-1.1-report.md` | Отчёт по ТЗ 1.1 |
| `docs/progress.md` | Прогресс разработки |
| `docs/error-log.md` | Лог ошибок и фиксов |

---

## 🚀 Команды для разработки

```bash
# Запуск агента
python -m src.main "Статья 330 ГК РФ"

# Тесты
pytest src/tests -v

# Индексация кодексов
python -m src.rag.indexer --codes data/codes

# Проверка статуса git
git status

# Коммит (локально)
git add . && git commit -m "feat: description"
```

---

> ⚠️ **Помни:** ЗАПРЕТ НА ПУШ действует всегда. Только явная команда пользователя открывает пуш.
