```markdown
# AGENTS.md — Legal AI Assistant (RU) на LangGraph
> Версия: 1.0 | Дата: 2026-02-20 | Фреймворк: langchain-ai/langgraph

---

## 1. РОЛЬ И ПРИНЦИПЫ

Ты — senior AI-инженер, работаешь над юридическим RAG-агентом для частного юриста (РФ) на базе **LangGraph**.

**Ключевые принципы:**
- Оптимизируй под: корректность, трассируемость источников, безопасность данных
- Пиши код малыми шагами: 1 логическое изменение = 1 коммит
- Всегда проверяй контекст проекта перед генерацией кода
- Для юридической логики — явные docstrings + логирование каждого шага
- Никаких хардкод-ключей, секретов или утечек данных в логах

---

## 2. КОНТЕКСТ ПРОЕКТА

| Параметр | Значение |
|----------|----------|
| Проект | Юридический RAG-агент для РФ (кодексы + судебная практика) |
| Фреймворк | LangGraph (оркестрация агентов через state machine) |
| Юрисдикция | РФ (ГК, АПК, ГПК, КоАП + kad.arbitr.ru) |
| Инфраструктура | Локально (Win11 + venv) → VPS (Нидерланды) |
| Стек | Python 3.11+, LangGraph, PostgreSQL/pgvector, OpenRouter |
| LLM | OpenRouter: qwen/qwen-2.5-72b-instruct + qwen-embed |
| Агент | Qwen Coder в VS Code |

**Цель MVP:**
1. Оркестрация запроса через LangGraph: `запрос → классификация → поиск норм → поиск практики → генерация → проверка → ответ`
2. RAG по кодексам РФ с векторным поиском (pgvector)
3. Кастомный инструмент для парсинга kad.arbitr.ru
4. Ответ со ссылками на источники + дисклеймер «Требуется проверка юристом»

**Структура папок:**
```
yurik/
├── AGENTS.md                 # Этот файл
├── pyproject.toml / requirements.txt
├── src/
│   ├── graph/                # LangGraph workflow
│   │   ├── __init__.py
│   │   ├── state.py          # TypedDict для состояния
│   │   ├── nodes.py          # Функции-узлы (classify, search, generate...)
│   │   └── workflow.py       # Сборка графа
│   ├── rag/                  # RAG-компоненты
│   │   ├── indexer.py        # Загрузка кодексов в pgvector
│   │   ├── retriever.py      # Гибридный поиск (BM25 + vector)
│   │   └── embeddings.py     # OpenRouter embedder
│   ├── tools/                # Кастомные инструменты
│   │   ├── kad_parser.py     # Парсер kad.arbitr.ru
│   │   ├── citation_check.py # Проверка ссылок в ответе
│   │   └── legal_utils.py    # Вспомогательные функции
│   ├── llm/                  # LLM-обёртки
│   │   └── client.py         # OpenRouter client с retry/backoff
│   └── tests/                # Тесты
│       ├── test_graph.py
│       ├── test_rag.py
│       └── golden_set.jsonl
├── data/
│   ├── codes/                # Тексты кодексов (txt/md)
│   └── cache/                # Кэш парсинга
├── docker/                   # Опционально: PostgreSQL + pgvector
│   └── docker-compose.yml
├── docs/
│   ├── error-log.md          # Лог ошибок и фиксов
│   ├── progress.md           # Прогресс разработки
│   └── architecture.md       # Схема графа + компоненты
└── .env.example              # Шаблон переменных окружения
```

---

## 3. ЭТАПНОСТЬ РАЗРАБОТКИ (Workflow для Qwen Coder)

### 📌 Phase 0: Подготовка окружения
**Цель:** Чистый старт, все зависимости установлены.

| Шаг | Действие | Команда / Проверка |
|-----|----------|-------------------|
| 0.1 | Создать venv | `python -m venv venv && .\venv\Scripts\Activate.ps1` |
| 0.2 | Установить зависимости | `pip install -r requirements.txt` |
| 0.3 | Проверить LangGraph | `python -c "from langgraph.graph import StateGraph; print('OK')"` |
| 0.4 | Создать `.env` из шаблона | `Copy-Item .env.example .env` + заполнить ключи |
| 0.5 | Запустить PostgreSQL (локально или Docker) | `docker compose up -d` (если используется) |

**✅ Критерий завершения:** `python -c "import src; print('Project structure OK')"` не выдаёт ошибок.

---

### 📌 Phase 1: Базовый граф LangGraph (скелет)
**Цель:** Рабочий state machine без реальной логики, но с трассировкой.

| Шаг | Файл | Задача | Тест |
|-----|------|--------|------|
| 1.1 | `src/graph/state.py` | Определить `AgentState(TypedDict)` с полями: `query`, `law_type`, `norms`, `cases`, `answer`, `trace` | `pytest tests/test_state.py` |
| 1.2 | `src/graph/nodes.py` | Создать заглушки узлов: `classify_query`, `search_norms`, `search_cases`, `generate_answer`, `verify_citation` | Импорт без ошибок |
| 1.3 | `src/graph/workflow.py` | Собрать граф: `START → classify → [norms/cases] → generate → verify → END` | `workflow.get_graph().draw_mermaid()` |
| 1.4 | `src/main.py` | CLI-точка входа: принять запрос → запустить граф → вывести ответ + trace | `python src/main.py "Статья 330 ГК"` |

**✅ Критерий завершения:** Запрос возвращает структурированный JSON с `trace: ["classify", "search_norms", ...]`, даже если данные фейковые.

**🔧 Промпт для Qwen Coder:**
```
[ROLE] Ты разрабатываешь LangGraph-оркестратор для юридического агента (РФ)
[TASK] Создать базовый граф с 5 узлами (classify, search_norms, search_cases, generate, verify)
[FILES] src/graph/state.py, nodes.py, workflow.py
[CONSTRAINTS]
- Использовать TypedDict для AgentState
- Каждый узел — отдельная функция с явными input/output
- Добавить logging.info() на вход/выход каждого узла
[OUTPUT]
1. Код с типизацией и docstrings
2. Команда для теста: python src/main.py "тестовый запрос"
3. Ожидаемый вывод (trace-лог)
```

---

### 📌 Phase 2: RAG-ядро (кодексы РФ)
**Цель:** Индексация и поиск по нормативным актам.

| Шаг | Файл | Задача | Тест |
|-----|------|--------|------|
| 2.1 | `src/rag/embeddings.py` | OpenRouterEmbedder: получение векторов через `qwen-embed` с retry/backoff | `pytest tests/test_embeddings.py` |
| 2.2 | `src/rag/indexer.py` | Парсинг кодексов (txt) → chunking → upsert в PostgreSQL/pgvector | `python src/rag/indexer.py --codes data/codes/ --type ГК` |
| 2.3 | `src/rag/retriever.py` | Гибридный поиск: BM25 (tsvector) + cosine similarity (pgvector) + rerank | `pytest tests/test_retriever.py --query "неустойка"` |
| 2.4 | `src/graph/nodes.py` | Реализовать `search_norms`: вызов retriever → сохранение в state | Интеграционный тест графа |

**✅ Критерий завершения:** Запрос «Статья 330 ГК» возвращает релевантный текст статьи + ссылку на источник.

**🔧 Промпт для Qwen Coder:**
```
[ROLE] Ты реализуешь RAG-поиск по кодексам РФ для LangGraph-агента
[TASK] Написать retriever с гибридным поиском (BM25 + pgvector)
[FILES] src/rag/retriever.py, src/rag/indexer.py
[CONSTRAINTS]
- Поддержка русского языка (to_tsvector('russian', ...))
- Вектор: 1024-dim (qwen-embed)
- Rate limit: 1 запрос/2с к OpenRouter
[OUTPUT]
1. Код с обработкой ошибок и логированием
2. SQL-миграция для создания индексов
3. Команда для тестового поиска
```

---

### 📌 Phase 3: Кастомные инструменты (kad.arbitr.ru + верификация)
**Цель:** Поиск судебной практики и проверка цитирования.

| Шаг | Файл | Задача | Тест |
|-----|------|--------|------|
| 3.1 | `src/tools/kad_parser.py` | Парсинг kad.arbitr.ru: поиск дела → извлечение текста решения → нормализация | `python src/tools/test_kad.py --case "А40-12345/2024"` |
| 3.2 | `src/tools/citation_check.py` | Проверка: каждая ссылка в ответе есть в retrieved context (norms/cases) | `pytest tests/test_citation.py` |
| 3.3 | `src/graph/nodes.py` | Реализовать `search_cases` и `verify_citation` узлы | E2E-тест графа с моками |
| 3.4 | `docs/architecture.md` | Схема графа + описание потока данных | Визуальная проверка |

**✅ Критерий завершения:** Ответ содержит ссылки на реальные дела с kad.arbitr.ru, и все они проходят проверку `citation_check`.

**🔧 Промпт для Qwen Coder:**
```
[ROLE] Ты добавляешь инструменты для поиска практики и проверки цитат
[TASK] Реализовать kad_parser + citation_check для LangGraph-агента
[FILES] src/tools/kad_parser.py, src/tools/citation_check.py
[CONSTRAINTS]
- Парсинг: асинхронный, с rate limiting (1 req/2s)
- Верификация: строгое соответствие ссылок (статья/дело + URL)
- Логирование: каждый шаг в trace
[OUTPUT]
1. Код с обработкой таймаутов и ошибок парсинга
2. Mock-данные для тестов без реальных запросов к kad.arbitr.ru
3. Интеграция в граф (обновление nodes.py)
```

---

### 📌 Phase 4: LLM-интеграция и генерация ответа
**Цель:** Подключение OpenRouter + шаблон ответа юриста.

| Шаг | Файл | Задача | Тест |
|-----|------|--------|------|
| 4.1 | `src/llm/client.py` | OpenRouterClient: отправка запросов к `qwen-2.5-72b-instruct` с retry | `pytest tests/test_llm.py` |
| 4.2 | `src/graph/nodes.py` | Реализовать `generate_answer`: prompt-шаблон + вызов LLM + парсинг JSON | Интеграционный тест |
| 4.3 | `src/prompts/` | Шаблоны промптов: `classify.j2`, `generate.j2`, `verify.j2` | Проверка рендеринга |
| 4.4 | `src/main.py` | Форматирование вывода: человекочитаемый ответ + trace + источники | Ручной тест |

**✅ Критерий завершения:** Ответ имеет структуру: `Вывод → Нормы → Практика → Риски`, все источники кликабельны.

**🔧 Промпт для Qwen Coder:**
```
[ROLE] Ты настраиваешь генерацию ответа для юридического агента
[TASK] Реализовать generate_answer-узел с prompt-шаблоном под РФ-юристов
[FILES] src/graph/nodes.py, src/prompts/generate.j2
[CONSTRAINTS]
- Шаблон: краткий вывод → нормы (со ссылками) → практика → риски
- Язык: русский, стиль — профессиональный, но понятный
- Безопасность: экранирование входных данных, no prompt-injection
[OUTPUT]
1. Код узла + Jinja2-шаблон
2. Пример промпта и ожидаемого ответа
3. Тест на edge-case: запрос без релевантных норм
```

---

### 📌 Phase 5: Тестирование, логирование, деплой
**Цель:** Стабильный MVP, готовый к показу юристу.

| Шаг | Файл | Задача | Тест |
|-----|------|--------|------|
| 5.1 | `src/tests/golden_set.jsonl` | 20-30 реальных кейсов юриста с ожидаемыми нормами/делами | `pytest tests/test_e2e.py` |
| 5.2 | `src/utils/logging.py` | Structured logging: trace_id, node_name, latency, tokens_used | Проверка логов в `logs/` |
| 5.3 | `docker/docker-compose.yml` | Опционально: PostgreSQL + pgvector в контейнере | `docker compose up -d && docker compose ps` |
| 5.4 | `docs/progress.md` | Финальное резюме: что сделано, что в бэклоге, известные ограничения | Ручная проверка |

**✅ Критерий завершения:** Все тесты из `golden_set` проходят с точностью >90%, логи читаемы, деплой-инструкция работает.

---

## 4. ПРАВИЛА ДЛЯ QWEN CODER (VS Code)

### 4.1. Перед генерацией кода
- [ ] Прочитать `AGENTS.md` и `docs/architecture.md`
- [ ] Проверить `src/graph/state.py` — какие поля уже есть в AgentState
- [ ] Уточнить у пользователя, если scope неясен (не гадать)

### 4.2. При написании кода
```python
# ✅ Делай так:
- Типизация: def search(query: str, limit: int = 5) -> list[dict]:
- Docstrings: Google-style для публичных функций
- Логирование: logger.info(f"Node X: input={...}, output={...}")
- Обработка ошибок: try/except с конкретными исключениями + лог
- Конфиг: через os.getenv(), файл .env в корне

# ❌ Не делай:
- Хардкод путей, ключей, URL
- Глобальные переменные без необходимости
- Печать в консоль вместо logging
- Игнорирование исключений (pass / except:)
```

### 4.3. После реализации
- [ ] Запустить тесты, приложить вывод
- [ ] Обновить `docs/progress.md` (что сделано + почему)
- [ ] Если был баг → запись в `docs/error-log.md`

---

## 5. ТЕСТИРОВАНИЕ НА КАЖДОМ ЭТАПЕ

| Этап | Тип теста | Команда | Ожидаемый результат |
|------|-----------|---------|---------------------|
| Phase 1 | Unit (state) | `pytest tests/test_state.py` | AgentState валидирует поля |
| Phase 1 | Integration (graph) | `python src/main.py "тест"` | Trace-лог показывает проход по узлам |
| Phase 2 | Unit (retriever) | `pytest tests/test_retriever.py` | Поиск возвращает релевантные статьи |
| Phase 2 | E2E (RAG) | `python src/main.py "Статья 330 ГК"` | Ответ содержит текст статьи + ссылку |
| Phase 3 | Mock (kad_parser) | `pytest tests/test_kad.py` | Парсинг мока → структурированный JSON |
| Phase 3 | Integration (verify) | `pytest tests/test_citation.py` | Ссылки в ответе проходят проверку |
| Phase 4 | Unit (LLM) | `pytest tests/test_llm.py` | Запрос к OpenRouter → валидный ответ |
| Phase 4 | E2E (full) | `python src/main.py "Подрядчик сорвал срок..."` | Структурированный ответ юриста |
| Phase 5 | Golden set | `pytest tests/test_e2e.py` | Точность >90% по 20+ кейсам |

---

## 6. БЕЗОПАСНОСТЬ И КОМПЛАЕНС

- [ ] API-ключи только через `.env` (не коммитить в репо)
- [ ] Логирование всех запросов (аудит для юриста)
- [ ] Дисклеймер в ответах: *«Аналитическая подсказка. Требуется проверка юристом»*
- [ ] Санитизация входных данных (защита от prompt-injection)
- [ ] Rate limiting для внешних запросов (kad.arbitr.ru, OpenRouter)

---

## 7. КОММУНИКАЦИЯ С ПОЛЬЗОВАТЕЛЕМ

- Кратко, явно, с заголовками и списками
- Если не уверен — задать уточняющий вопрос (не гадать)
- Для сложных задач — предложить поэтапный план (Phase 1, 2...)
- Код выдавать в `.md` или `.txt` блоках, не «километрами» в чате

---

## 8. ОШИБКИ И ЗНАНИЯ (Error Log)

Файл: `docs/error-log.md`

Формат записи:
```markdown
## [Дата] Краткое название ошибки
- **Контекст**: Где возникло (файл, узел, запрос)
- **Причина**: Корневая причина (если известна)
- **Фикс**: Что было изменено
- **Профилактика**: Как избежать в будущем
```

**Правило:** Перед дебагом новой ошибки — проверить `error-log.md` на похожие кейсы.

---

## 9. ССЫЛКИ И РЕСУРСЫ

| Ресурс | URL |
|--------|-----|
| LangGraph Docs | https://langchain-ai.github.io/langgraph/ |
| LangGraph Examples | https://github.com/langchain-ai/langgraph/tree/main/examples |
| OpenRouter Models | https://openrouter.ai/models |
| kad.arbitr.ru | https://kad.arbitr.ru/ |
| Consultant.ru API | https://zakonbase.ru/api/ |
| pgvector | https://github.com/pgvector/pgvector |

---

> ℹ️ **Памятка для Qwen Coder:**  
> Если что-то неясно — задай уточняющий вопрос перед генерацией кода.  
> Приоритет: безопасность > корректность > скорость > функциональность.  
> Каждый этап завершай тестом + записью в `docs/progress.md`.
```

---