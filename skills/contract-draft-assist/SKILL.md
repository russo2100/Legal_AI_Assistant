---
name: contract-draft-assist
description: Помощь в создании и проверке юридических документов РФ: договоры, иски, претензии, ходатайства, отзывы. Используй, когда пользователь просит создать черновик, проверить документ на реквизиты, добавить пункт о неустойке/подсудности, или улучшить формулировки. Не используй для общих вопросов без контекста документа.
license: MIT
metadata:
  author: Ruslan
  version: 1.0.0
  jurisdiction: RU
  framework: langgraph
  doc-types: [contract, claim, motion, response, agreement_addendum]
  requires-logging: true
---

# Contract Draft Assist (РФ)

Навык для создания, проверки и улучшения черновиков юридических документов в соответствии с законодательством РФ. Работает в связке с LangGraph-оркестратором и модулем аудита.

## Когда активировать этот навык

✅ **Используй, когда пользователь:**
- Просит создать черновик документа («составь договор подряда», «напиши претензию», «подготовь иск»)
- Запрашивает проверку документа на реквизиты/соответствие закону
- Хочет добавить/изменить пункт («добавь неустойку 0.1% в день», «включи арбитражную оговорку»)
- Просит улучшить формулировки («сделай этот пункт юридически грамотнее»)
- Запрашивает шаблон по конкретному типу спора

❌ **Не используй, когда:**
- Вопрос не связан с документами (общие консультации — используй `legal-research`)
- Пользователь не указал тип документа и контекст (запроси уточнение)
- Запрос требует анализа судебной практики без черновика (сначала `legal-research`)

---

## 🔄 Рабочий процесс (Workflow)

При активации навыка следуй этому алгоритму:

### Шаг 1: Определение типа документа
Определи категорию и подкатегорию:
```
Типы: договор | иск | претензия | ходатайство | отзыв | доп. соглашение
Подкатегории: подряд | аренда | поставка | купля-продажа | услуги | труд
```

### Шаг 2: Сбор контекста
Запроси недостающие данные (если не указаны):
- Стороны документа (кто исполнитель/заказчик, истец/ответчик)
- Предмет и существенные условия
- Сумма/сроки/ответственность (если применимо)
- Применимое право и подсудность

### Шаг 3: Генерация черновика
Используй шаблон из `templates/` + LLM-генерацию:
```python
# Псевдокод
from src.llm.client import get_llm_client
from src.llm.prompts import render_prompt

client = get_llm_client()
template = render_prompt(
    f"templates/{doc_type}.j2",
    context=user_context,
    clauses=clause_library.get(doc_type)
)
draft = client.generate(prompt=template, temperature=0.2)
```

### Шаг 4: Валидация реквизитов
Вызови `src/tools/legal_utils.py`:
- Проверка обязательных реквизитов (ст. 432 ГК РФ для договоров)
- Формат сумм, дат, подписей
- Соответствие ГПК/АПК для процессуальных документов

### Шаг 5: Проверка цитирования
Если в документе есть ссылки на нормы:
- Вызови `src/tools/citation_check.py`
- Убедись, что все статьи существуют и актуальны

### Шаг 6: Логирование и вывод
Запиши в аудит-лог:
- Тип документа, пользователь, timestamp
- Использованные шаблоны и нормы
- Статус валидации

Выведи пользователю:
```
📄 ЧЕРНОВИК: [Название документа]

[Текст документа с форматированием]

✅ ПРОВЕРКА:
- Обязательные реквизиты: [OK / Требуется дополнение]
- Ссылки на нормы: [Все верны / Требуют проверки]
- Формат: [Соответствует ГПК/АПК / Есть замечания]

📋 СЛЕДУЮЩИЕ ШАГИ:
1. Проверить реквизиты сторон
2. Уточнить [недостающий пункт]
3. Подписать и направить

> ⚠️ Дисклеймер: Черновик требует проверки юристом перед использованием.
```

---

## 📋 Примеры использования

### Пример 1: Создание договора подряда
**Пользователь:** «Составь черновик договора подряда на ремонт офиса, сумма 500 тыс., срок 30 дней, неустойка 0.1% в день»

**Действия:**
1. Тип: договор, подкатегория: подряд
2. Контекст собран из запроса
3. Шаблон `templates/contract-podryad.j2` + генерация
4. Валидация: реквизиты сторон, предмет, цена, сроки, ответственность
5. Логирование: audit.log записывает создание черновика
6. Вывод: готовый черновик + чек-лист проверки

**Ожидаемый вывод:**
```
📄 ЧЕРНОВИК: Договор подряда № ___ от «__» ______ 20__ г.

[Полный текст договора с разделами: предмет, сроки, цена, порядок оплаты, ответственность, форс-мажор, реквизиты]

✅ ПРОВЕРКА:
- Обязательные реквизиты: OK (предмет, цена, сроки указаны)
- Неустойка: 0.1%/день — соответствует ст. 330 ГК РФ
- Формат: Соответствует требованиям ГК РФ

📋 СЛЕДУЮЩИЕ ШАГИ:
1. Вписать реквизиты сторон (наименование, ИНН, адрес)
2. Уточнить порядок приёмки работ (акт/подписание)
3. Добавить приложения (смета, тех. задание)

> ⚠️ Дисклеймер: Черновик требует проверки юристом перед использованием.
```

### Пример 2: Проверка иска на реквизиты
**Пользователь:** «Проверь этот иск на соответствие АПК: [текст]»

**Действия:**
1. Тип: иск, категория: арбитраж
2. Валидация по ст. 125 АПК РФ (обязательные элементы иска)
3. Проверка ссылок на нормы через `citation_check`
4. Вывод: список замечаний + исправленный вариант

### Пример 3: Добавление пункта в договор
**Пользователь:** «Добавь в договор пункт о подсудности в АС г. Москвы»

**Действия:**
1. Запросить контекст: какой договор, текущий текст
2. Сгенерировать формулировку по ст. 37 АПК РФ
3. Вставить в документ с сохранением структуры
4. Проверить на конфликт с другими пунктами

---

## 📚 Ссылки на ресурсы (references/)

Перед генерацией документа проконсультируйся с этими файлами:

| Файл | Назначение |
|------|------------|
| `references/mandatory-requisites.md` | Обязательные реквизиты по типам документов (ГК, АПК, ГПК) |
| `references/clause-library.md` | Библиотека типовых пунктов: неустойка, форс-мажор, конфиденциальность |
| `references/formatting-rules.md` | Требования к оформлению: шрифты, нумерация, подписи |
| `references/pitfalls-by-type.md` | Частые ошибки по типам документов и как их избежать |

---

## 🔐 Логирование и аудит (критично для юриста)

Каждое действие навыка должно записываться в аудит-лог:

```python
# Псевдокод логирования
from src.utils.logging_config import get_audit_logger

audit = get_audit_logger()
audit.log(
    event="document_draft_created",
    user_id=user_id,
    doc_type=doc_type,
    doc_subtype=doc_subtype,
    templates_used=[template_name],
    norms_referenced=[article_list],
    validation_status=validation_result,
    timestamp=datetime.now().isoformat()
)
```

**Что логировать обязательно:**
- [ ] Тип документа и категория спора
- [ ] Использованные шаблоны и нормы
- [ ] Результат валидации реквизитов
- [ ] Статус проверки цитирования
- [ ] Timestamp и user_id (для traceability)

**Формат записи в `logs/audit.log`:**
```json
{
  "timestamp": "2026-02-20T14:30:00Z",
  "event": "document_draft_created",
  "user": "lawyer_123",
  "doc": {"type": "contract", "subtype": "podryad"},
  "templates": ["contract-podryad.j2"],
  "norms": ["ГК ст. 330", "ГК ст. 702"],
  "validation": {"requisites": "ok", "citations": "ok"},
  "trace_id": "abc123"
}
```

---

## ⚠️ Troubleshooting

### Проблема: Документ не проходит валидацию реквизитов
**Симптом:** `legal_utils.validate_requisites()` возвращает ошибки  
**Решение:**
1. Проверь `references/mandatory-requisites.md` — все ли обязательные поля заполнены
2. Если пользователь не указал данные — запроси уточнение перед генерацией
3. В выводе явно укажи: «Требуется дополнить: [список полей]»

### Проблема: LLM генерирует некорректные формулировки
**Симптом:** Пункты договора противоречат ГК РФ или не соответствуют практике  
**Решение:**
1. Убедись, что в промпте есть инструкция: «Используй только актуальные нормы ГК/АПК/ГПК РФ»
2. Добавь few-shot примеры корректных формулировок в шаблон
3. После генерации — обязательная валидация через `legal_utils`

### Проблема: Конфликт пунктов в документе
**Симптом:** Новый пункт противоречит существующему (например, две разные подсудности)  
**Решение:**
1. Перед вставкой — проверить документ на наличие конфликтных норм
2. Если конфликт найден — предложить пользователю выбор: заменить или удалить старый пункт
3. Записать предупреждение в аудит-лог

---

## 🧪 Тестирование навыка

Перед использованием в production проверь на этих кейсах:

```bash
# 1. Генерация договора
python src/main.py --skill contract-draft-assist "Составь договор аренды нежилого помещения"

# 2. Проверка иска
python src/main.py --skill contract-draft-assist "Проверь иск на соответствие АПК: [текст]"

# 3. Добавление пункта
python src/main.py --skill contract-draft-assist "Добавь неустойку 0.1% в договор поставки"

# 4. Edge case: без контекста
python src/main.py --skill contract-draft-assist "Напиши договор"
# Ожидаемо: запрос уточнения типа и сторон
```

**Критерии успеха:**
- [ ] Документ содержит все обязательные реквизиты по типу
- [ ] Ссылки на нормы проверены и валидны
- [ ] Запись в аудит-лог создана с trace_id
- [ ] Дисклеймер на русском в конце
- [ ] Время генерации <15 секунд (для черновика)

---

## 🔗 Интеграция с другими навыками

| Навык | Сценарий совместного использования |
|-------|-----------------------------------|
| `legal-research` | Сначала поиск норм/практики → затем генерация документа на их основе |
| `case-strategy-workflow` | Анализ кейса → формирование стратегии → черновик иска/отзыва |

**Пример цепочки:**
```
User: «Подрядчик сорвал срок, как взыскать?»
→ legal-research: находит ст. 330, 723 ГК + практику
→ contract-draft-assist: генерирует претензию/иск на основе найденного
→ citation_check: проверяет все ссылки в документе
→ audit.log: полная трассировка для отчётности
```

---

## 📁 Структура папки навыка

```
skills/contract-draft-assist/
├── SKILL.md                      # Этот файл
├── templates/
│   ├── contract-base.j2          # Базовый шаблон договора
│   ├── contract-podryad.j2       # Договор подряда
│   ├── contract-arenda.j2        # Договор аренды
│   ├── contract-postavka.j2      # Договор поставки
│   ├── claim-arbitration.j2      # Иск в арбитраж
│   ├── claim-general.j2          # Иск в суд общей юрисдикции
│   ├── pretenziya.j2             # Досудебная претензия
│   └── motion.j2                 # Ходатайство/отзыв
├── references/
│   ├── mandatory-requisites.md   # Обязательные реквизиты
│   ├── clause-library.md         # Библиотека пунктов
│   ├── formatting-rules.md       # Требования к оформлению
│   └── pitfalls-by-type.md       # Частые ошибки
├── scripts/
│   ├── validate_requisites.py    # Проверка реквизитов
│   └── check_conflicts.py        # Поиск конфликтных пунктов
└── assets/
    ├── disclaimer-ru.txt         # Дисклеймер
    └── signature-block.md        # Шаблон блока подписей
```

---

> ℹ️ **Памятка:** Этот навык — часть LangGraph-оркестратора. Не дублируй логику `legal-research` — вызывай его при необходимости поиска норм перед генерацией документа. Все действия логируй в `logs/audit.log` с trace_id для последующего аудита.
